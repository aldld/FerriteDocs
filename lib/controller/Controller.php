<?php

/**
 * Main FerriteDocs functionality is handled through controllers.
 * It is through this class that controllers are loaded and
 * managed.
 *
 * When the load() method is called, the specified controller is
 * by including the controller file associated with the name specified.
 */
class Controller
{
    public static function load($name) {
    }
}
